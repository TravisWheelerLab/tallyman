/// The default ratio of the number of slots in the hash
/// to the number of DCE sequences we want to store.
pub const HASH_CAPACITY_MULTIPLE: usize = 8;

/// Size for all the buffers we use for reading FASTA files.
pub const BUFFER_SIZE: usize = 4096;
